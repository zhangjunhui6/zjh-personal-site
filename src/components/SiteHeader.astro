---
import { site } from '@/config/site';

const currentPath = Astro.url.pathname;
---

<header class="container flex flex-col items-start gap-4 py-6 sm:flex-row sm:items-center sm:justify-between">
  <a class="text-lg font-semibold no-underline" href="/" aria-label="返回首页">
    {site.name}
  </a>
  <nav class="flex flex-wrap items-center gap-4 text-sm text-[var(--color-muted)] sm:justify-end" aria-label="主导航">
    {site.nav.map((item) => {
      const isActive = currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href));

      return (
        <a
          class:list={[
            'no-underline transition-colors',
            isActive ? 'text-[var(--color-ink)]' : 'hover:text-[var(--color-leaf)]',
          ]}
          href={item.href}
          aria-current={isActive ? 'page' : undefined}
        >
          {item.label}
        </a>
      );
    })}
  </nav>
</header>
